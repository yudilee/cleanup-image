# Default Backend URL (Optional - defaults to /api)
# Set this to your stable Vercel deployment of the backend or a dedicated server.
NEXT_PUBLIC_API_URL=/api

# Google Colab / High-Priority Backend URL (Optional)
# If set, the app will try to connect to this first.
# If this fails (e.g., Colab runtime stopped), it will automatically fallback to NEXT_PUBLIC_API_URL.
NEXT_PUBLIC_COLAB_URL=https://your-ngrok-url.ngrok-free.app
